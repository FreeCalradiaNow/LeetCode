#pragma once
#include <iostream>
#include <vector>
#include "1480RunningSum.h"

using std::vector;

int main()
{
    Solution s;
    vector<int> nums = { 1, 2, 3, 4 };
    vector<int> result = s.runningSum(nums);
    cout << runningSum << endl;
    return 0;
}